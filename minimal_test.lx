void main()
{
    int x = 5;
    int y = 2;
    int z = 0;
    int u = 0;
    z = x + y;
    u = x - y;
    if(z > 6)
    {
        lulog(z);
    }

    if(u < 3)
    {
        lulog(u);
    }
    luloop(x > 0)
    {
        lulog(x);
        x = x - 1;
    }
}