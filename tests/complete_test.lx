void main()
{
    int a = 1;
    int b = 1;
    int c = 5;
    int temp = a + b + c;
    lulog(temp);
    
    temp = 1 + 2 + 3;
    lulog(temp);

    temp = 4 / 2;
    lulog(temp);

    temp = 2 * 2 * 2;
    5
    temp = 5;
    luloop(temp > 0)
    {
        if(temp == 1)
        {
            lulog(58);
        }
        else
        {
            lulog(temp);
        }
        temp = temp - 1;
    }
    int a = luload();
    if(a >= 5)
    {
        lulog(a);
    }
    
    if(a != 1)
    {
        lulog(6);
    }
}